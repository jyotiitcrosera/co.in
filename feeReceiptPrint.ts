function amountInWords(value: number) {
  const n = Math.max(0, Math.round(Number(value) || 0));
  if (n === 0) return "Zero rupees only";
  const ones = ["", "One", "Two", "Three", "Four", "Five", "Six", "Seven", "Eight", "Nine", "Ten", "Eleven", "Twelve", "Thirteen", "Fourteen", "Fifteen", "Sixteen", "Seventeen", "Eighteen", "Nineteen"];
  const tens = ["", "", "Twenty", "Thirty", "Forty", "Fifty", "Sixty", "Seventy", "Eighty", "Ninety"];
  const under100 = (x: number) => x < 20 ? ones[x] : `${tens[Math.floor(x / 10)]}${x % 10 ? ` ${ones[x % 10]}` : ""}`;
  const under1000 = (x: number) => `${x >= 100 ? `${ones[Math.floor(x / 100)]} Hundred${x % 100 ? " " : ""}` : ""}${x % 100 ? under100(x % 100) : ""}`.trim();
  const parts: string[] = [];
  if (Math.floor(n / 10000000)) parts.push(`${under100(Math.floor(n / 10000000))} Crore`);
  const lakh = Math.floor((n % 10000000) / 100000);
  if (lakh) parts.push(`${under100(lakh)} Lakh`);
  const thousand = Math.floor((n % 100000) / 1000);
  if (thousand) parts.push(`${under100(thousand)} Thousand`);
  const rest = n % 1000;
  if (rest) parts.push(under1000(rest));
  return `${parts.join(" ")} rupees only`;
}

export function buildFeeReceiptHtml(receipt: any, date = new Date().toLocaleDateString("en-IN")) {
  const final = Boolean(receipt?.fullyPaid);
  const esc = (value: unknown) => String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/\"/g, "&quot;");
  const amount = Number(receipt?.paymentAmount || 0);
  const totalPaid = Number(receipt?.totalPaid || amount);
  const balance = Number(receipt?.balance || 0);
  const money = (value: number) => `₹${Math.round(value || 0).toLocaleString("en-IN")}`;
  const title = final ? "FULLY PAID FEE RECEIPT" : "FEE RECEIPT";
  const rows = ["Admission Fee", "Development Fee", "Caution Money", "Training Fee", "Late Fee / Fine", "Library Fee", "Sports Fee", "Examination Fee", "Education Fee", "Misc. Charges"];
  const history = Array.isArray(receipt?.paymentHistory) ? receipt.paymentHistory : [];
  const historyRows = history.map((payment: any[], index: number) => { const item: any = payment; return `<tr><td>${index + 1}</td><td>${esc(item?.[1] || item?.date || "—")}</td><td>${esc(item?.[0] || item?.invoiceNo || "—")}</td><td>₹${esc(Number(item?.[7] ?? item?.paymentAmount ?? 0).toLocaleString("en-IN"))}</td><td>₹${esc(Number(item?.[8] ?? item?.totalPaid ?? 0).toLocaleString("en-IN"))}</td><td>₹${esc(Number(item?.[9] ?? item?.balance ?? 0).toLocaleString("en-IN"))}</td></tr>`; }).join("");
  const historySection = history.length ? `<section class="history"><h3>Installment Payment History</h3><table><thead><tr><th>Sl.</th><th>Date</th><th>Invoice No.</th><th>Amount Paid</th><th>Total Paid</th><th>Balance</th></tr></thead><tbody>${historyRows}</tbody></table></section>` : "";
  return `<!doctype html><html><head><meta charset="utf-8"><title>${title}</title><style>
  @page{size:A4 portrait;margin:10mm}*{box-sizing:border-box}body{font-family:Arial,Helvetica,sans-serif;color:#172238;margin:0;background:#fff}.receipt{width:190mm;min-height:277mm;margin:0 auto;border:1.2px solid #172238;padding:7mm 8mm;position:relative}.receipt:before{content:"";position:absolute;inset:3mm;border:0.6px solid #8d98a8;pointer-events:none}.inner{position:relative;z-index:1}.topline{display:flex;justify-content:space-between;align-items:flex-start;font-size:9px;font-weight:700}.institute{text-align:center;margin:1mm 0 2mm}.institute h1{font-family:Georgia,serif;font-size:23px;letter-spacing:.04em;margin:0;text-transform:uppercase}.institute p{margin:1mm 0;font-size:9px}.institute .affiliation{font-size:8px;color:#566174}.receipt-title{display:flex;align-items:center;justify-content:center;gap:8mm;margin:4mm 0 3mm;border-top:1.5px solid #172238;border-bottom:1.5px solid #172238;padding:2.5mm 0;font-size:16px;font-weight:800;letter-spacing:.12em}.status{font-size:8px;letter-spacing:.08em;border:1px solid #172238;padding:1.5mm 2.5mm}.student-meta{display:grid;grid-template-columns:1.15fr 1fr 1fr;gap:0;border:1px solid #172238;margin-top:3mm}.meta-cell{min-height:13mm;padding:2mm 2.5mm;border-right:1px solid #172238;border-bottom:1px solid #172238}.meta-cell:nth-child(3n){border-right:0}.meta-cell:nth-last-child(-n+3){border-bottom:0}.label{font-size:8px;font-weight:700;color:#566174;text-transform:uppercase;letter-spacing:.08em}.value{font-size:11px;font-weight:700;margin-top:1mm;min-height:4mm}.fee-table{width:100%;border-collapse:collapse;margin-top:5mm;font-size:10px;table-layout:fixed}.fee-table th,.fee-table td{border:1px solid #172238;padding:2.2mm 2.5mm;height:8.4mm}.fee-table th{background:#eef1f4;text-transform:uppercase;font-size:8px;letter-spacing:.08em}.fee-table th:first-child,.fee-table td:first-child{width:12%}.fee-table th:nth-child(2),.fee-table td:nth-child(2){width:68%;text-align:left}.fee-table th:last-child,.fee-table td:last-child{width:20%;text-align:right}.fee-table .total-row td{font-weight:800;background:#f4e5ad}.fee-table .paid-row td{font-weight:800;background:#eef3f7}.history{margin-top:5mm}.history h3{margin:0;padding:2mm 0;font-size:10px;text-transform:uppercase;letter-spacing:.08em}.history table{width:100%;border-collapse:collapse;font-size:8.5px}.history th,.history td{border:1px solid #172238;padding:1.8mm 2mm}.history th{background:#eef1f4;text-transform:uppercase;font-size:7px}.receipt-summary{display:grid;grid-template-columns:repeat(3,1fr);margin-top:4mm;border:1px solid #172238}.summary-cell{padding:2.5mm;border-right:1px solid #172238;min-height:12mm}.summary-cell:last-child{border-right:0}.summary-label{display:block;font-size:8px;font-weight:700;text-transform:uppercase;color:#566174}.summary-value{display:block;margin-top:1mm;font-size:11px;font-weight:800}.amount-words{border:1px solid #172238;border-top:0;padding:2.5mm;font-size:9px;font-weight:700;min-height:11mm}.lower{display:grid;grid-template-columns:1.4fr 1fr;margin-top:7mm;border:1px solid #172238;min-height:29mm}.lower-left{padding:3mm;border-right:1px solid #172238;font-size:9px;line-height:1.8}.lower-right{display:flex;align-items:flex-end;justify-content:center;padding:3mm;text-align:center;font-size:9px;font-weight:700}.signature-line{border-top:1px solid #172238;padding-top:2mm;width:48mm}.footer-note{margin-top:4mm;text-align:center;font-size:8px;color:#566174}.print-footer{position:absolute;bottom:5mm;left:8mm;right:8mm;display:flex;justify-content:space-between;font-size:7px;color:#566174}@media print{body{background:#fff}.receipt{margin:0;width:190mm;min-height:277mm;border:1.2px solid #172238;box-shadow:none}}
  </style></head><body><main class="receipt"><div class="inner"><div class="topline"><span>JYOTI ITC, ROSERA</span><span>Receipt No.: ${esc(receipt?.invoiceNo)}</span></div><header class="institute"><h1>JYOTI ITC</h1><p>Industrial Training Institute, Rosera, Samastipur (Bihar)</p><p class="affiliation">Govt. of India · Affiliation: DGET-6/4/71/2010-TC</p></header><div class="receipt-title">${title}<span class="status">${final ? "FULLY PAID" : "PAID"}</span></div><section class="student-meta"><div class="meta-cell"><span class="label">Receipt No.</span><div class="value">${esc(receipt?.invoiceNo)}</div></div><div class="meta-cell"><span class="label">Name</span><div class="value">${esc(receipt?.studentName)}</div></div><div class="meta-cell"><span class="label">Trade</span><div class="value">${esc(receipt?.trade)}</div></div><div class="meta-cell"><span class="label">Roll No.</span><div class="value">${esc(receipt?.registrationNo || receipt?.rollNo || receipt?.roll)}</div></div><div class="meta-cell"><span class="label">Year / Session</span><div class="value">${esc(receipt?.year || receipt?.session)}</div></div><div class="meta-cell"><span class="label">Date</span><div class="value">${esc(date)}</div></div></section><table class="fee-table"><thead><tr><th>Sl. No.</th><th>Particulars</th><th>Amount (Rs.)</th></tr></thead><tbody>${rows.map((label, index) => `<tr><td>${index + 1}</td><td>${label === "Admission Fee" ? `${label}${receipt?.remarks ? ` — ${esc(receipt.remarks)}` : ""}` : label}</td><td>${index === 0 ? money(amount) : ""}</td></tr>`).join("")}<tr class="paid-row"><td colspan="2">Amount Paid on This Receipt</td><td>${money(amount)}</td></tr><tr class="total-row"><td colspan="2">Grand Total</td><td>${money(amount)}</td></tr></tbody></table>${historySection}<div class="receipt-summary"><div class="summary-cell"><span class="summary-label">Admission Fee</span><span class="summary-value">${money(Number(receipt?.admissionFee || 0))}</span></div><div class="summary-cell"><span class="summary-label">Total Paid To Date</span><span class="summary-value">${money(totalPaid)}</span></div><div class="summary-cell"><span class="summary-label">Balance / Status</span><span class="summary-value">${money(balance)} · ${esc(receipt?.paymentStatus || (final ? "FULLY PAID" : "PARTIALLY PAID"))}</span></div></div><div class="amount-words">In words: ${esc(amountInWords(amount))}</div><div class="lower"><div class="lower-left"><div><b>By Cash / Cheque No.:</b> ${esc(receipt?.paymentMode || "CASH")}</div><div><b>Bank Name:</b> ________________________________</div><div><b>Date:</b> ${esc(date)}</div></div><div class="lower-right"><div class="signature-line">Sign. of Fee Collector</div></div></div><p class="footer-note">This receipt is issued against the amount received by JYOTI ITC. Please retain it for your records.</p></div><div class="print-footer"><span>JYOTI ITC · Official Fee Receipt</span><span>${final ? "FULLY PAID" : "Keep for records"}</span></div></main><script>window.addEventListener('load',()=>setTimeout(()=>window.print(),250))<\/script></body></html>`;
}
